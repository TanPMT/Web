# Production Environment Variables
DB_HOST=mysql
DB_NAME=notes_app
DB_USER=notes_user
DB_PASSWORD=change-this-password-in-production

# IMPORTANT: Change this to a secure random string in production!
JWT_SECRET=change-this-secret-key-to-random-string-in-production

# App Environment
APP_ENV=production
